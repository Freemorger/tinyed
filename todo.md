# now
- rename (remove te_ in filenames)
- dynamic inputfield size (and outline mb)
- focusing into widgets
- `GFX_Color` struct
- `gfx_draw_line_ex` 
- better looking inputfield outline maybe
- maybe do `TE_Vec_remove` by predicate instead
# later
